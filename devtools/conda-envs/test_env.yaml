name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - numpy
  - pandas

  # molsystem requires, and only available via Conda.
  - openbabel

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - pathvalidate
    - pycifrw

    # Testing
    - yapf

