name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - numpy

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - pathvalidate
      # Testing
    - yapf
    - codecov

