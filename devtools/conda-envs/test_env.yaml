name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - numpy
  - pandas

  # molsystem requires, and only available via Conda.
  - seamm-util
  
  - openbabel
  - pathvalidate
  - pycifrw
  - rdkit
  - seekpath
  - spglib

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike
