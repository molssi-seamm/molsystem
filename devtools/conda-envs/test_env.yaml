name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip
  - numpy
  - pandas

  # molsystem requires, and only available via Conda.
  - seamm_util
  
  - openbabel
  - pathvalidate
  - pycifrw
  - rdkit
  - seekpath
  - spglib

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
