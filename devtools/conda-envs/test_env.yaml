name: test
channels:
  - default
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - configargparse
  - numpy
  - pandas
  # Module dependencies
  - openbabel

  # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - pathvalidate
      # Testing
    - yapf
    - codecov

